services:
  content-admin:
    build:
      context: .
      args:
        VITE_CONTENT_SERVER_URL: http://lynn2:8080
    container_name: content-admin
    ports:
      - "8082:80"
    restart: unless-stopped
